// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'table_cart.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class TableCart extends HookWidget {
  const TableCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableCart(_context);
}

class FloatCart extends HookWidget {
  const FloatCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => floatCart(_context);
}

class FloatCartSelectedSource extends HookWidget {
  const FloatCartSelectedSource({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => floatCartSelectedSource();
}

class FloatCartCount extends HookWidget {
  const FloatCartCount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => floatCartCount(_context);
}

class ExpandedCountTitle extends HookWidget {
  const ExpandedCountTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => expandedCountTitle();
}

class ExpandedTableCart extends HookWidget {
  const ExpandedTableCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => expandedTableCart(_context);
}

class TableCartItems extends HookWidget {
  const TableCartItems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableCartItems(_context);
}

class TableCartItem extends HookWidget {
  const TableCartItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableCartItem();
}
