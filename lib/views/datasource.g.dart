// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'datasource.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class DatasourcePage extends HookWidget {
  const DatasourcePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourcePage(_context);
}

class DatasourceFragment extends HookWidget {
  const DatasourceFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceFragment();
}

class AddDatasourceItem extends HookWidget {
  const AddDatasourceItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => addDatasourceItem(_context);
}

class DatasourceItem extends HookWidget {
  const DatasourceItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceItem(_context);
}

class DatasourceHeader extends StatelessWidget {
  const DatasourceHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceHeader();
}

class NormalHeader extends StatelessWidget {
  const NormalHeader(this.title, {Key? key}) : super(key: key);

  final String title;

  @override
  Widget build(BuildContext _context) => normalHeader(title);
}

class DatasourceDetailPage extends StatelessWidget {
  const DatasourceDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceDetailPage();
}

class TableSections extends HookWidget {
  const TableSections({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableSections(_context);
}

class TableItemProviderScope extends HookWidget {
  const TableItemProviderScope({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableItemProviderScope();
}

class DatasourceDetailHeader extends HookWidget {
  const DatasourceDetailHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceDetailHeader();
}

class DatasourceEditPage extends StatelessWidget {
  const DatasourceEditPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceEditPage(_context);
}

class DatasourceEditHeader extends HookWidget {
  const DatasourceEditHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceEditHeader();
}

class DatasourceEditForm extends HookWidget {
  const DatasourceEditForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => datasourceEditForm(_context);
}

class TableSearchBar extends HookWidget {
  const TableSearchBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableSearchBar(_context);
}

class TableFilterResult extends HookWidget {
  const TableFilterResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableFilterResult();
}

class DbFilters extends HookWidget {
  const DbFilters({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => dbFilters(_context);
}

class TableItem extends HookWidget {
  const TableItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => tableItem(_context);
}
