// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'panel.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class MenuPanel extends HookWidget {
  const MenuPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => menuPanel();
}

class NavRail extends HookWidget {
  const NavRail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => navRail(_context);
}

class NavRailLeading extends HookWidget {
  const NavRailLeading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => navRailLeading(_context);
}

class NavRailTopIcon extends HookWidget {
  const NavRailTopIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => navRailTopIcon(_context);
}

class ContentPanel extends HookWidget {
  const ContentPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => contentPanel();
}

class ControlButtonGroup extends HookWidget {
  const ControlButtonGroup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => controlButtonGroup(_context);
}

class ControlButton extends HookWidget {
  const ControlButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => controlButton(_context);
}

class ContentPanelNavigator extends StatelessWidget {
  const ContentPanelNavigator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext _context) => contentPanelNavigator();
}
